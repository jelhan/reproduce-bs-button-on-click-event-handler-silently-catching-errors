{{page-title "ReproduceBsButtonOnClickEventHandlerSilentlyCatchingErrors"}}

<BsButton @onClick={{this.syncThrow}}>
  sync @onClick handler, which throws
</BsButton>

<BsButton @onClick={{this.asyncThrow}}>
  async @onClick handler, which throws
</BsButton>